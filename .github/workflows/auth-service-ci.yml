name: Auth Service CI

on:
  push:
    paths:
      - "logpulse_backend/scripts/services/auth-service/**"
      - "!logpulse_backend/scripts/services/auth-service/README.md"
  pull_request:
    paths:
      - "logpulse_backend/scripts/services/auth-service/**"
      - "!logpulse_backend/scripts/services/auth-service/README.md"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r logpulse_backend/scripts/services/auth-service/requirements.txt
      - name: Run tests
        run: |
          cd logpulse_backend/scripts/services/auth-service
          python3 -m pytest
      - name: Build Docker image
        run: |
          docker build -t auth-service:ci ./logpulse_backend/scripts/services/auth-service
