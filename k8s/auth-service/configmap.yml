apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
data:
  DATABASE_URL: postgresql+asyncpg://auth_user:password@postgres:5432/auth_db
  JWT_SECRET: replace-me
  ACCESS_TOKEN_EXPIRE_MINUTES: "30"
